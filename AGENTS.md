# SharedAgentSkills Repository Adapter

The repository-maintenance rules live in `.agents/AGENTS.md`.
If you have not already loaded that file, read it completely before editing this repository.

The `.agents/` and `.claude/` trees and this root adapter are repository-local only.
They are never installed into any harness's global configuration.

Before handing work back: python tools/validate_skills.py
